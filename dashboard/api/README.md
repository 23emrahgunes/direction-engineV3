# Dashboard API

The V3.13 dashboard API is served by `direction_engine_v3.app.server`.

Read-only endpoints:

- `/health/live`
- `/health/ready`
- `/metrics`
- `/api/dashboard`

The service binds to `127.0.0.1` by default and exposes no mutation endpoint.
